$('.favorite-<%= @favorite.book_id %>').html("<%= j(render 'favorites/btn',  book: @favorite.book ) %>")
<!--インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できる-->
<!--("#book_%= @book.id % .favorite-btn")はidがbook_〇の要素の中の、favorite-btnクラスの要素」を指す-->